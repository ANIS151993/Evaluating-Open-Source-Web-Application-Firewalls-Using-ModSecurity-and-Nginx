# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk

# Lab run outputs (regenerate with lab/run_full_evaluation.sh)
lab/results/*
!lab/results/.gitkeep

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Duplicate figure copies live at repo root from earlier drafts; the
# canonical, site-referenced copies are under docs/assets/figures/.
/fig*.png

# Working notes containing a live GitHub access token -- never commit this.
/Prompt.md

# Original oversized infographic; the optimized, web-sized copy actually
# used by the site/README lives at docs/assets/infographic.jpg.
/Open-Source_Firewall_Benefits_Infographic.png
